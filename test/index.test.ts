import { expect } from 'chai';

describe('Sample Tests', function () {
    it('First Sample', function () {
        expect(true).to.equal(true);
    });

    it('Second Sample', function () {
        expect(true).to.equal(false);
    });
});
